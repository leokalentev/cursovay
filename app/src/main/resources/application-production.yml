spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cursovay
    username: postgres
    password: Leonigleo1024@+
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem

